server:
  port: 1111

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cadcli
    username: fabianocampos
    password: campos
  jpa:
    generate-ddl: true
    show-sql: true
  sql:
    init:
      mode: always